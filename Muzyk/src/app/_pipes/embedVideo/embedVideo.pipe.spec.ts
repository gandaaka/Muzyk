import { TestBed, async } from '@angular/core/testing';
import { EmbedVideoPipe } from './embedVideo.pipe';

describe('Pipe: EmbedVideoe', () => {
  it('create an instance', () => {
    const pipe = new EmbedVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
